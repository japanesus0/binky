// Placeholder so `flutter test` runs cleanly. Add real widget tests here
// when needed — most logic in this app is intentionally in plain Dart
// classes that are easy to test directly without pumping the widget tree.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(true, isTrue);
  });
}
